<div class="container jumbotron" style="margin-top:15px;">
    <div class="row">Derniers Articles :</div>
    <div class="row">

        {% for article in articles  %}
        <div class="col-xs-4">
            <a href="{{path('blog_detail',{id:article.id})}}">{{ article.titre }}</a>
        </div>
        {% endfor %}

    </div>
</div>